/*
 * Como criar um objeto
 *  - no JS podemos criar um objeto abrindo e fechando chaves
 *  - o objeto parece um array de chave e valor
 *  - podemos atribuir o objeto a uma variável
*/

let carro = {
  portas: 4,
  cor: "Cinza"
}

let pessoa = {
  nome: "Letícia"
}

console.log(typeof carro) // object
