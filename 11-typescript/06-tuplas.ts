/*
 * Outros tipos de dados do TS
 *  - o TS nos provê outros tipos de dados, como: tuplas, Enum, Any, Void, Null e Undefined e Never
 *  - a tupla serve para determinarmos um conjunto de dados, com tipos específicos e fixos, para uma variável
*/

let pessoa: [string, number];
pessoa = ['Luiz', 30];
